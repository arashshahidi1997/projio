---
title: "Flatten pipe/flow: update docs paths, collection, modkey, and agent instructions"
date: 2026-04-01
timestamp: 20260401-043426-424405
status: pending
actionable: true
prompt: ""
source_note: "task-arash-20260401-043425-765707"
project_primary: "projio"
tags: [task, pipeio, refactor, docs, instructions]
---

# Flatten pipe/flow: update docs paths, collection, modkey, and agent instructions

## Goal

Update all documentation, docs collection paths, modkey bibliography format, CLAUDE.md files, agent instructions codegen, and spec docs to reflect flow-only addressing.

## Context

Prior tasks changed: registry schema (flow-only keys), all MCP functions (dropped pipe), CLI (dropped pipe), projio wrappers (dropped pipe). This is the final cleanup task — update everything that references the old pipe/flow model.

## Prompt

> Update all documentation and agent-facing content for flow-only addressing.
>
> ### 1. Docs collection paths (`packages/pipeio/src/pipeio/docs.py`)
>
> Update `docs_collect()`:
> - Target path: `docs/pipelines/{flow}/` (was `docs/pipelines/{pipe}/{flow}/`)
> - Mod docs: `docs/pipelines/{flow}/mods/{mod}.md`
> - Notebooks: `docs/pipelines/{flow}/notebooks/nb-{name}.html` and `.md`
> - Scripts: `docs/pipelines/{flow}/scripts/{script}.py` (new — copy rule scripts for site display)
> - Flow-local source: `<flow_dir>/docs/` → `docs/pipelines/{flow}/`
>
> Update `docs_nav()`:
> - Nav structure mirrors the new flat `docs/pipelines/{flow}/` layout
> - Reference `entry.effective_derivative` where derivative grouping is needed
>
> ### 2. Modkey bibliography (`packages/pipeio/src/pipeio/mcp.py`)
>
> Update `mcp_modkey_bib()`:
> - Old citekey: `pipe-{pipe}_flow-{flow}_mod-{mod}`
> - New citekey: `flow-{flow}_mod-{mod}`
> - Update title, note fields to match
> - This is a **breaking change for existing citations** — document it
>
> ### 3. Agent instructions (`src/projio/init.py`)
>
> Update `_generate_claude_md()`:
> - Tool routing table: remove all `pipe` params from examples
> - Notebook workflow section: update all tool call examples
> - All `(pipe, flow, name)` references → `(flow, name)`
>
> ### 4. Spec docs (`docs/specs/pipeio/`)
>
> Update these files:
> - `docs/specs/pipeio/notebook.md` — remove pipe from all examples and YAML schemas
> - `docs/specs/pipeio/mcp-tools.md` — update all function signatures, remove pipe params
> - `docs/specs/pipeio/cli.md` — update CLI command signatures
> - `docs/specs/pipeio/scaffolding.md` — update flow new examples
> - `docs/specs/pipeio/overview.md` — update terminology (pipe → derivative where relevant)
>
> ### 5. CLAUDE.md files
>
> Update:
> - `CLAUDE.md` (projio root) — tool listing, remove pipe from pipeio tools
> - `packages/pipeio/CLAUDE.md` — tool listing, update counts, remove pipe references
>
> ### 6. Existing task notes
>
> Update the flow rename task note (`docs/log/task/task-arash-20260331-231724-488753.md`) to reflect flow-only addressing.
>
> ### Constraints
> - Run both pipeio tests (`cd packages/pipeio && PYTHONPATH=src python -m pytest tests/ -q`) and projio tests (`PYTHONPATH=src python -m pytest tests/ -q`)
> - Save with `datalad_save` (recursive, include both packages/pipeio and projio root files)

## Acceptance Criteria

- [ ] `docs_collect()` produces `docs/pipelines/{flow}/` structure
- [ ] `docs_nav()` generates nav for flat layout
- [ ] Modkey citekeys use `flow-{flow}_mod-{mod}` format
- [ ] Agent instructions reference flow-only addressing everywhere
- [ ] All spec docs updated
- [ ] Both CLAUDE.md files updated
- [ ] All tests pass (both pipeio and projio)

## Result

(Filled in after execution)
