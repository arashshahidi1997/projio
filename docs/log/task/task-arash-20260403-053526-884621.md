---
title: "Implement Shibboleth SAML login for biblio auth ezproxy --login"
date: 2026-04-03
timestamp: 20260403-053526-884621
status: done
result_note: /storage2/arash/worklog/workflow/captures/20260403-060146-2258f2/note.md
completed: 2026-04-03T06:01:48+02:00
actionable: true
prompt: |
  Implement programmatic Shibboleth SAML login for `biblio auth ezproxy --login`.

  Context: LMU Munich EZProxy requires Shibboleth authentication. The SAML flow has been mapped:
  1. GET `https://emedien.ub.uni-muenchen.de/login?url=...` → 302 + session cookies
  2. POST SAML request to `https://lmuidp.lrz.de/idp/profile/SAML2/POST/SSO`
  3. LRZ IdP presents username/password form
  4. POST credentials → IdP returns SAML response
  5. POST SAML response back to EZProxy → authenticated session cookies

  Implementation plan:
  1. Add `shibboleth_login(proxy_base, username, password)` to `packages/biblio/src/biblio/ezproxy.py`
     - Use `requests.Session()` to follow the full redirect chain
     - Use `beautifulsoup4` to parse the IdP login form fields
     - POST credentials, follow SAML response back to EZProxy
     - Return the authenticated cookie string (ezproxy=...; ezproxyl=...; ezproxyn=...)

  2. Add `--login` flag to `biblio auth ezproxy` in `packages/biblio/src/biblio/cli.py`
     - Prompt for LMU username via `input()`
     - Prompt for password via `getpass.getpass()` (hidden)
     - Call `shibboleth_login()`, save cookies to `~/.config/biblio/config.yml`
     - Set `chmod 600`

  3. Update `packages/biblio/docs/how-to/institutional-access.md` with the new `--login` flow

  4. Make `requests` and `beautifulsoup4` optional deps — graceful error if missing

  Test by running `biblio auth ezproxy --login` on the HPC and verifying
  `biblio bibtex fetch-pdfs-oa` can fetch a paywalled paper (e.g. buzsaki2014, DOI 10.1038/nrn3687).
source_note: ""
project_primary: projio
tags: [task, biblio, auth, ezproxy]
---

# Implement Shibboleth SAML login for biblio auth ezproxy --login

## Goal

Eliminate manual cookie management for institutional PDF access. Users should be able to authenticate with their LMU credentials from a terminal (no browser needed) and have biblio handle the SAML flow automatically.

## Context

- LMU EZProxy at `emedien.ub.uni-muenchen.de` uses Shibboleth SAML2 via `lmuidp.lrz.de`
- HPC server is on LMU network but IP-based auto-auth is NOT enabled
- Current approach requires browser login + cookie copy (expires every few hours)
- SAML flow was traced on 2026-04-03 — standard POST binding, form-based IdP login

## Acceptance Criteria

- [ ] `biblio auth ezproxy --login` prompts for username + password (hidden), does SAML flow, saves cookies
- [ ] `biblio bibtex fetch-pdfs-oa` successfully fetches a paywalled paper after `--login`
- [ ] Works over plain SSH (no display/browser required)
- [ ] `requests` and `beautifulsoup4` are optional — graceful error message if missing
- [ ] Tutorial updated in `institutional-access.md`

## Result

(Filled in after execution)
