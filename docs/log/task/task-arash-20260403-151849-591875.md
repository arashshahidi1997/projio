---
title: "biblio: pool-aware derivative resolution (docling, grobid, openalex from shared storage)"
date: 2026-04-03
timestamp: 20260403-151849-591875
status: pending
actionable: true
prompt: |
  Implement pool-aware derivative resolution in biblio so that docling, grobid, and
  openalex outputs are checked in shared storage before running locally.

  Context: SiroCampus has a shared pool at /storage/share/sirocampus/bib/ that contains
  PDFs and derivatives (docling, grobid, openalex). Currently biblio only checks the pool
  for PDFs (during fetch_pdfs_oa via pool.search paths). Docling, GROBID, and OpenAlex
  outputs are regenerated per-project even when identical outputs exist in the shared pool.

  Design:

  1. Extend pool.search to cover derivatives, not just PDFs:
     - pool.search already lists paths like ["/storage/share/sirocampus/bib", "/storageN/user/bib"]
     - For each pool path, also check derivatives/docling/, derivatives/grobid/, derivatives/openalex/
     - Symlink or read from pool when found, skip local generation

  2. Two-tier derivative model:
     SHARED (pool-level, symlink from pool, never regenerate locally):
       - bib/articles/{citekey}/{citekey}.pdf
       - bib/derivatives/docling/{citekey}/*.md, *.json
       - bib/derivatives/grobid/{citekey}/*.xml, *.json
       - bib/derivatives/openalex/cache/{doi_slug}.json
       - bib/derivatives/html/{citekey}/*.html

     PROJECT-LOCAL (always generated per-project, project-specific):
       - bib/config/library.yml (status, tags, priority)
       - bib/config/collections.yml
       - bib/derivatives/summaries/ (LLM-generated, project-curated)
       - bib/derivatives/reviews/ (LLM-generated, project-curated)
       - bib/derivatives/reading-lists/
       - bib/derivatives/cite-drafts/
       - .projio/indexio/ (RAG index, project-specific corpus)

  3. Key functions to modify:
     - outputs_for_key() in docling.py — check pool paths before local
     - grobid_outputs_for_key() in grobid.py — check pool paths before local
     - run_docling_for_key() — skip if pool output exists (unless force=True)
     - run_grobid_for_key() — skip if pool output exists (unless force=True)
     - find_pending_docling() in batch.py — exclude keys with pool outputs
     - find_pending_grobid() — exclude keys with pool outputs

  4. Pool resolution helper:
     def resolve_derivative(cfg, citekey, derivative_type) -> Path | None:
         Check pool search paths for an existing derivative.
         Returns the path if found in any pool, None otherwise.
         for pool_root in cfg.pool_search:
             candidate = pool_root / "derivatives" / derivative_type / citekey
             if candidate.exists():
                 return candidate
         return None

  5. Status awareness:
     - paper_context() should report derivative_source: pool | local | missing
     - library_quality() should distinguish pool vs local derivatives
     - batch operations should show "N from pool, M to process locally"

  6. Config: pool.search already exists. Add pool.derivatives: true (default) to enable.
     pool.derivatives_types: ["docling", "grobid", "openalex"] to control which types.

  Test: In sirocampus, verify that docling batch skips papers whose outputs already
  exist in /storage/share/sirocampus/bib/derivatives/docling/. Verify paper_context()
  reports derivative_source correctly.
source_note: ""
project_primary: projio
tags: [task, biblio, pool, derivatives, architecture]
---

# biblio: pool-aware derivative resolution

## Goal

Stop regenerating docling/grobid/openalex outputs that already exist in the shared pool. This saves compute time, API calls, and storage — the shared pool at `/storage/share/sirocampus/bib/` already has derivatives for most papers.

## Context

- Pool PDF search works (`pool.search` in config, used by `fetch_pdfs_oa`)
- Derivatives are NOT pool-aware — every project regenerates them independently
- SiroCampus pool has docling outputs for 250+ papers
- Running `docling batch` on a new project re-processes all of them (~45s each = 3+ hours wasted)

## Two-tier model

| Tier | Examples | Resolution |
|------|----------|------------|
| Shared | PDFs, docling, grobid, openalex | Check pool first, symlink if found |
| Project-local | summaries, reviews, reading lists, RAG | Always generate per-project |

## Acceptance Criteria

- [ ] `resolve_derivative(cfg, citekey, "docling")` checks pool paths
- [ ] `run_docling_for_key` skips when pool output exists (unless `force=True`)
- [ ] `find_pending_docling` excludes keys with pool derivatives
- [ ] `paper_context` reports `derivative_source: "pool" | "local" | "missing"`
- [ ] Batch operations show pool skip counts
- [ ] Config: `pool.derivatives: true` (default enabled)

## Result

(Filled in after execution)
