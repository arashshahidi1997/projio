---
title: "Flatten pipe/flow: update all pipeio MCP functions"
date: 2026-04-01
timestamp: 20260401-043424-441698
status: pending
actionable: true
prompt: ""
source_note: "task-arash-20260401-043322-873085"
project_primary: "projio"
tags: [task, pipeio, refactor, mcp]
---

# Flatten pipe/flow: update all pipeio MCP functions

## Goal

Update every `mcp_*` function in `packages/pipeio/src/pipeio/mcp.py` to use flow-only addressing. Remove all `pipe: str` parameters. The registry was already updated in the prior task to use flow-name keys and `FlowEntry.derivative` instead of `FlowEntry.pipe`.

## Context

The prior task (task-arash-20260401-043322-873085) changed the registry schema: `FlowEntry.pipe` → `FlowEntry.derivative`, keys from `"pipe/flow"` to `"flow"`, `registry.get(flow)` instead of `registry.get(pipe, flow)`.

This task updates ALL functions in `packages/pipeio/src/pipeio/mcp.py` that currently take `pipe: str` and `flow: str` params.

## Prompt

> Update all MCP functions in `packages/pipeio/src/pipeio/mcp.py` to drop the `pipe` parameter.
>
> ### What changed in the registry (prior task)
> - `FlowEntry.pipe` → `FlowEntry.derivative` (optional, defaults to flow name)
> - `FlowEntry` has `effective_derivative` property
> - `registry.get(flow)` instead of `registry.get(pipe, flow)`
> - `registry.remove(flow)` instead of `registry.remove(pipe, flow)`
> - `registry.list_flows(derivative=None)` instead of `list_flows(pipe=None)`
> - Registry keys are flow names, not "pipe/flow"
>
> ### Functions to update (all in mcp.py)
>
> **Flow/registry tools:**
> - `mcp_flow_list(root, pipe=None)` → `mcp_flow_list(root, derivative=None)`
> - `mcp_flow_status(root, pipe, flow)` → `mcp_flow_status(root, flow)`
> - `mcp_flow_deregister(root, pipe, flow)` → `mcp_flow_deregister(root, flow)`
> - `mcp_flow_fork(root, pipe, flow, new_flow, new_pipe=None)` → `mcp_flow_fork(root, flow, new_flow, derivative=None)`
>
> **Notebook tools (all take pipe, flow, name → just flow, name):**
> - `mcp_nb_status(root, pipe, flow, name)` → `mcp_nb_status(root, flow=None, name=None)` (derivative filter replaces pipe filter)
> - `mcp_nb_update(root, pipe, flow, name, ...)` → drop pipe
> - `mcp_nb_sync(root, pipe, flow, name, ...)` → drop pipe
> - `mcp_nb_sync_flow(root, pipe, flow, ...)` → drop pipe
> - `mcp_nb_diff(root, pipe, flow, name)` → drop pipe
> - `mcp_nb_read(root, pipe, flow, name)` → drop pipe
> - `mcp_nb_publish(root, pipe, flow, name, ...)` → drop pipe
> - `mcp_nb_create(root, pipe, flow, name, ...)` → drop pipe
> - `mcp_nb_exec(root, pipe, flow, name, ...)` → drop pipe
> - `mcp_nb_analyze(root, pipe, flow, name)` → drop pipe
> - `mcp_nb_lab(root, pipe, flow, ...)` → drop pipe (use derivative for lab grouping)
>
> **Mod/rule/config tools:**
> - `mcp_mod_list(root, pipe, flow)` → drop pipe
> - `mcp_mod_resolve(root, modkeys)` → update modkey format from `pipe-X_flow-Y_mod-Z` to `flow-Y_mod-Z`
> - `mcp_mod_context(root, pipe, flow, mod)` → drop pipe
> - `mcp_mod_create(root, pipe, flow, mod, ...)` → drop pipe
> - `mcp_rule_list(root, pipe, flow)` → drop pipe
> - `mcp_rule_stub(root, pipe, flow, ...)` → drop pipe
> - `mcp_rule_insert(root, pipe, flow, ...)` → drop pipe
> - `mcp_rule_update(root, pipe, flow, ...)` → drop pipe
> - `mcp_config_read(root, pipe, flow)` → drop pipe
> - `mcp_config_patch(root, pipe, flow, ...)` → drop pipe
> - `mcp_config_init(root, pipe, flow, ...)` → drop pipe
> - `mcp_target_paths(root, pipe, flow, ...)` → drop pipe
>
> **Other tools:**
> - `mcp_completion(root, pipe, flow)` → drop pipe
> - `mcp_cross_flow(root, pipe)` → `mcp_cross_flow(root, derivative=None)`
> - `mcp_dag_export(root, pipe, flow, ...)` → drop pipe
> - `mcp_log_parse(root, pipe, flow)` → drop pipe
> - `mcp_run(root, pipe, flow, ...)` → drop pipe
> - `mcp_run_status(root, run_id)` → unchanged (already flow-independent)
> - `mcp_run_dashboard(root)` → unchanged
> - `mcp_run_kill(root, run_id)` → unchanged
> - `mcp_modkey_bib(root, ...)` → update citekey format
> - `mcp_docs_collect(root)` → update path computation
> - `mcp_docs_nav(root)` → update path computation
>
> ### Pattern for each function
> Where it currently does:
> ```python
> entry = registry.get(pipe, flow)
> ```
> Change to:
> ```python
> entry = registry.get(flow)
> ```
> And where it references `entry.pipe`, change to `entry.effective_derivative` or `entry.derivative`.
>
> Where it builds paths like `docs/pipelines/{pipe}/{flow}/`, change to `docs/pipelines/{flow}/`.
>
> ### Also update:
> - `_resolve_mod_doc_path(root, pipe, flow, mod)` → `_resolve_mod_doc_path(root, flow, mod)` — use `docs/pipelines/{flow}/mods/{mod}.md` pattern
> - Modkey format: `pipe-{pipe}_flow-{flow}_mod-{mod}` → `flow-{flow}_mod-{mod}`
>
> ### Constraints
> - Only modify `packages/pipeio/src/pipeio/mcp.py`
> - Run `PYTHONPATH=src python -m pytest tests/ -q` — fix any test failures
> - Save with `datalad_save`

## Acceptance Criteria

- [ ] No `pipe: str` parameter on any mcp_* function (except as `derivative` where needed)
- [ ] All `registry.get(pipe, flow)` calls updated to `registry.get(flow)`
- [ ] `entry.pipe` references changed to `entry.effective_derivative`
- [ ] Modkey format updated
- [ ] Doc paths simplified
- [ ] All tests pass

## Result

(Filled in after execution)
