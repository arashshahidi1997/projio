---
title: "Stack-axis layout/spec: handbook + workshop (from stack-axis survey)"
date: 2026-05-08
timestamp: 20260508-160001-200002
status: done
result_note: /storage2/arash/worklog/workflow/captures/20260508-162152-7c4026/note.md
completed: 2026-05-08T16:21:54+02:00
actionable: true
prompt: ""
source_note: docs/log/idea/idea-arash-20260507-221835-382557.md
project_primary: projio
priority: high
due: ""
goal: agentic-workshop-2026-09
blocked: false
blocked_by: ""
tags: [handbook, workshop, spec, stack-axis, agentic-workshop-2026-09]
supersedes: docs/log/task/task-arash-20260508-020131-100002.md
---

## Task

Produce the layout/spec for the handbook + workshop, **organized along the
stack-component axis** (BIDS → DataLad → Snakemake → Marimo → Quarto/MkDocs
→ projio → agentic). Supersedes the earlier substantive-axis spec at
`docs/log/result/result-arash-20260508-021908-627443.md` (kept on disk as
reference but no longer authoritative).

The handbook + workshop teach the *stack itself*, not analyses done with
it. projio is introduced gradually — workshop day 1 and early day 2 use
the stack without projio; projio is brought in as the layer that knows
about the conventions below it; agentic workflows are layered on top last.

## Inputs (read in full before drafting)

- Stack-axis survey (primary input):
  `docs/log/result/result-arash-20260508-stack-axis-survey.md`
- Idea note (architecture, frame, tooling):
  `docs/log/idea/idea-arash-20260507-221835-382557.md`
- Goal note (success criteria, milestones):
  `docs/log/goal/goal-arash-20260507-221912-674817.md`
- Prior substantive-axis spec for reference *only*:
  `docs/log/result/result-arash-20260508-021908-627443.md` — reuse the
  two-workspace contract (handbook in projio vs workshop in
  `teaching/agentic-workshop/`), the cross-link protocol, and the
  Quarto-multi-output decision; **do not** reuse the chapter list.

## Output

Two files — **both must be created on disk** (the prior task folded these
into one and skipped `docs/handbook/`; do not repeat that). Create
`docs/handbook/` if absent.

### File 1: `docs/handbook/_outline.md`

The handbook chapter list, stack-axis-organized.

Contents:

1. **Chapter list** — full ordered list, one line per chapter. Suggested
   structure (refine from survey evidence):

   - 00-frame: why this stack; why interactivity; single-author fragility
   - 10-bids: the data-layout convention (component 1)
   - 20-datalad: versioning data + code (component 2)
   - 30-snakemake: pipeline orchestration (component 3)
   - 40-marimo: reactive notebooks (component 4)
   - 50-publication: Quarto + MkDocs (component 5)
   - 60-projio: the stack-aware layer (component 6, **graded introduction
     across multiple sub-chapters** — codio, biblio, notio, figio,
     pipeio; introduced from the pain each solves in the prior stack)
   - 70-agentic: Claude Code, MCP, skills, captures → tasks → queues
     (component 7)
   - 80-orchestration: worklog cross-project (focus contracts, dispatch,
     goals, critical path)
   - 99-honest-gaps: consolidate survey gaps

2. **Per-chapter sub-section list** — one line each.
3. **Per-chapter mapping** — which stack component, which workshop session,
   which canonical artifact from the survey, whether it has a planned
   marimo-WASM explorable (max 5 across the handbook total — reuse the
   chapter-1 task-graph one and pick at most 4 more, S/M/L cost).
4. **Cross-link policy** (reuse from prior spec §F): handbook chapters
   cite source artifacts via repo-relative paths; workshop handouts link
   *into* handbook published URLs.
5. **Asset conventions** — figures (figio vs static), explorables, naming.
6. **Handbook ↔ workshop mapping table** — every handbook chapter to the
   workshop session(s) it pairs with, and vice versa.

### File 2: `docs/log/result/result-arash-20260508-stack-axis-syllabus-spec.md`

The workshop syllabus, stack-axis-organized.

Suggested per-day shape (refine from survey evidence — adjust if the
graded introduction sequence in survey component 6 implies a different
ordering):

- **Day 1 — Stack foundations (no projio yet, no agent yet)**
  - AM: BIDS + DataLad — strict layout, subdatasets, siblings
  - PM: Snakemake — snakebids; one rule chain end-to-end on a teaching
    dataset
- **Day 2 — Authoring + interactivity (still pre-projio for first
  half)**
  - AM: Marimo notebooks — reactive cells; analyses; introduce `nb_watch`
  - PM: Quarto + MkDocs — publication frameworks; reports + handbook;
    *introduce projio for the first time as the system that knows how to
    wire all of the above together*
- **Day 3 — projio + agentic**
  - AM: projio ecosystem — codio, biblio, notio, figio, pipeio; how each
    knows about the stack below it (pipeio knows about Snakemake + BIDS;
    figio knows about Quarto-rendered reports; notio knows about
    project memory)
  - PM: Agentic on top — Claude Code, MCP, skills, captures, queues,
    cross-project dispatch via worklog
- **Day 4 — Participant presentations + group discussion**

For each session: learning outcome (single sentence), prerequisite
knowledge, hands-on artifact the participant produces, dataset used, the
handbook chapter(s) it pairs with.

Also include in this file:

- Pre-workshop setup checklist skeleton (one bullet per item)
- Day-4 rubric skeleton
- Backup-plan skeleton (no internet, agent rate-limited, weird BYO data,
  install fails)

## Hard rules

- **Stack-axis only.** Chapter list is organized by stack component. No
  chapter named after a substantive analysis (no "ECoG preprocessing",
  no "spike sorting").
- **Spec only.** No chapter prose. No exercise solutions.
- **Ground in the stack-axis survey.** Every "example: X" claim traces to
  a specific entry in `result-arash-20260508-stack-axis-survey.md`. Mark
  `[example: TBD]` if the survey lacks one.
- **Both files must be written.** The prior task skipped `docs/handbook/`
  — do not repeat. If `docs/handbook/` doesn't exist, create it.
- **Don't write chapter prose for chapter 1.** The chapter-1 draft task
  remains separate (`task-arash-20260507-222028-504938.md`).
- **Don't provision the workshop workspace.** Separate task
  (`task-arash-20260507-221947-582556.md`).
- **Reuse from prior spec where on-axis** — the two-workspace contract,
  cross-link protocol, Quarto-multi-output, tooling-decision tables in
  prior spec §A and §E remain valid; cite them by reference rather than
  rewriting.

## Acceptance

- `docs/handbook/_outline.md` exists with chapter list, per-chapter
  sub-sections, paradigm/component/example mapping, asset conventions,
  mapping table, explorable shortlist
- `docs/log/result/result-arash-20260508-stack-axis-syllabus-spec.md`
  exists with full per-session breakdown
- Every chapter and session traces to a stack component
- Every example claim traces to the stack-axis survey
- No prose drafts written; outputs are purely structural
