# Spec TODOs

- Align derivative layout to `derivatives/<pipe>/flow-<flow>/...` across existing pipelines. (Note that this is not by direct enforcement but by updating the registry of the code/pipelines/ that produce those derivatives)
