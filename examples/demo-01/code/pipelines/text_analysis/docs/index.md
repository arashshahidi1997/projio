# text_analysis

Word frequency analysis pipeline for the demo-01 project.

## Overview

This flow reads raw text files and produces word frequency counts,
demonstrating the full pipeio ontology: flow → mod → rule → script → output.

## Modules

- **wordcount** — counts word frequencies and computes Zipf rank ordering
